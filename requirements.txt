# Core dependencies (already in pyproject.toml)
numpy
einops
Pillow
opencv-python
tqdm
pyyaml
plyfile
natsort

# SLAM-specific
pyrealsense2
evo
# torchcodec: optional (dataloader falls back gracefully); version must match torch exactly,
# e.g. torchcodec==0.1 for torch 2.5.x — install manually if video decoding speed matters

# Splatt3R-specific
lightning
lpips
omegaconf
huggingface_hub
gitpython
scikit-learn
scikit-image
# faiss: built from source with GPU support (thirdparty/faiss, see README),
# replaces faiss-gpu-cu12 which pins numpy<2


# Note: The following need to be installed separately:
# - PyTorch (see README.md for version matching CUDA)
# - lietorch: pip install git+https://github.com/princeton-vl/lietorch.git
# - diff-gaussian-rasterization: pip install -e thirdparty/diff-gaussian-rasterization-modified
# - thirdparty/in3d: pip install -e thirdparty/in3d
# - torchcodec (optional, version must match torch)
